# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# ── Generated pipeline data ──
# Regenerated by refresh_data.py. 265 MB and growing; scraper.log alone is 91 MB.
# Also contains bulk scraped data on identifiable traders — see README.
# Safe to ignore wholesale: polymarket_top_traders_scraper.py runs first in the
# refresh pipeline and does OUTPUT_DIR.mkdir(parents=True, exist_ok=True), so a
# fresh clone rebuilds the directory on first run.
PolyMarket_Code/output/

# ── Local tooling / editor ──
.claude/settings.local.json
.freebuff/
.vscode/
.idea/
.DS_Store
Thumbs.db

# ── Stray dir from a shell that did not expand ~ ──
/~/

# ── Secrets: none present today, guarding against future ──
.env
.env.*
*.key
*.pem
*.pfx
*.p12
credentials.json
secrets.json